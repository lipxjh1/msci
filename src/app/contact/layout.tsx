export const metadata = {
  title: "Contact | Overwatch Clone",
  description:
    "CONTACT US JOIN THE M-SCI COMMUNITY OR CONTACT OUR SUPPORT TEAM",
};

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
} 